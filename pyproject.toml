[project]
name = "eth-analyzer"
version = "0.1.0"
description = ""
authors = [
    {name = "klianchenkov.va",email = "klianchenkov.va@dozen.mephi.ru"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "eth_analyzer", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
